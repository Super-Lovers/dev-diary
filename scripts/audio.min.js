const audio=document.getElementById("audioPlayer");audio.volume=.001;const session=window.sessionStorage;let timeOfMusic=session.getItem("timeOfMusic");null==timeOfMusic?(session.setItem("timeOfMusic",0),audio.play()):(audio.currentTime=timeOfMusic+1,audio.play());const songDuration=audio.duration;setInterval(()=>{0==audio.paused&&((timeOfMusic=audio.currentTime)==songDuration&&(timeOfMusic=0),session.setItem("timeOfMusic",timeOfMusic))},1e3);